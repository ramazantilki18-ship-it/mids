rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm koleksiyonlara (audits, nonconformities, users)
    // giriş yapmış her kullanıcının erişimine izin ver
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
